---
---

<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Golf Site Scout: Spot Risks in Minutes, Save Thousands</title>
  <style>
    /* Global Styles */
    body {
      font-family: 'Lato', sans-serif;
      background: linear-gradient(to bottom, #2E7D32, #81C784);
      margin: 0;
      padding: 0;
      color: #333;
    }

    /* Hero Section */
    .hero {
      color: white;
      text-align: center;
      padding: 4rem 2rem;
      position: relative;
      overflow: hidden;
    }

    .hero::before {
      content: "";
      position: absolute;
      top: 0;
      left: 0;
      width: 100%;
      height: 100%;
      background-image: url('grass-texture.png'); /* Placeholder */
      background-repeat: repeat;
      opacity: 0.1;
      z-index: 0;
    }

    .hero-content {
      position: relative;
      z-index: 1;
    }

    .hero h1 {
      font-family: 'Open Sans', sans-serif;
      font-size: 48px;
      font-weight: bold;
      text-shadow: 2px 2px 4px #FFD700;
      margin-bottom: 1rem;
    }

    .hero p {
      font-size: 20px;
      color: lightgrey;
      margin-bottom: 2rem;
    }

    .hero button {
      background: linear-gradient(to right, #FF6D00, #FFA726);
      color: white;
      font-family: 'Montserrat', sans-serif;
      font-weight: bold;
      padding: 1rem 2rem;
      border: none;
      border-radius: 0.5rem;
      font-size: 1.2rem;
      cursor: pointer;
      transition: background-color 0.3s ease;
    }

    .hero button:hover {
      background: linear-gradient(to right, #FF8F00, #FFB347);
    }

    .launch-price {
      color: #FFD700;
      font-style: italic;
      display: block;
      margin-top: 0.5rem;
    }

    .map-container {
      border: 2px solid rgba(0, 0, 0, 0.1);
      border-radius: 0.5rem;
      margin-top: 2rem;
      position: relative;
      overflow: hidden;
    }

    .map-container::before {
      content: "Sample";
      position: absolute;
      top: 10px;
      left: 10px;
      font-family: 'Playfair Display', serif;
      font-style: italic;
      font-size: 2rem;
      color: rgba(255, 255, 255, 0.5);
      z-index: 1;
    }

    .map-placeholder {
      width: 100%;
      height: 300px;
      background-color: #f0f0f0;
      display: flex;
      justify-content: center;
      align-items: center;
      font-family: 'Playfair Display', serif;
      font-size: 2rem;
      color: #999;
    }

    .risk-layers {
      margin-top: 1rem;
      display: flex;
      justify-content: center;
      gap: 1rem;
    }

    .risk-layers button {
      background-color: #4CAF50;
      border: none;
      color: white;
      padding: 0.5rem 1rem;
      text-align: center;
      text-decoration: none;
      display: inline-block;
      font-size: 1rem;
      cursor: pointer;
      border-radius: 0.25rem;
    }

    /* Middle Section */
    .middle {
      padding: 2rem;
      text-align: center;
    }

    .proof-points {
      display: flex;
      justify-content: center;
      gap: 2rem;
      margin-bottom: 2rem;
    }

    .proof-point {
      display: flex;
      align-items: center;
      font-family: 'Roboto', sans-serif;
      font-size: 16px;
    }

    .proof-point::before {
      content: "●";
      color: #FFD700;
      margin-right: 0.5rem;
    }

    .trust-section {
      margin-bottom: 2rem;
    }

    .trust-section p {
      margin-bottom: 0.5rem;
    }

    .form-container {
      background-color: white;
      padding: 2rem;
      border-radius: 0.5rem;
      box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
      width: 80%;
      max-width: 600px;
      margin: 0 auto;
    }

    .form-container h2 {
      font-family: 'Merriweather', serif;
      margin-bottom: 1rem;
    }

    .form-container input[type="email"] {
      width: 100%;
      padding: 0.75rem;
      margin-bottom: 1rem;
      border: 2px solid #2E7D32;
      border-radius: 0.25rem;
    }

    .form-container button {
      background-color: #FFD700;
      color: #333;
      border: none;
      padding: 0.75rem 1.5rem;
      border-radius: 0.25rem;
      cursor: pointer;
      font-weight: bold;
    }

    .testimonial {
      font-family: 'Playfair Display', serif;
      font-style: italic;
      color: #777;
      padding: 1rem;
      border-radius: 0.5rem;
      background-color: #f9f9f9;
      margin-top: 2rem;
    }

    /* Footer Section */
    .footer {
      text-align: center;
      padding: 2rem;
    }

    /* Mobile Styles */
    @media (max-width: 600px) {
      .hero h1 {
        font-size: 36px;
      }

      .hero button {
        font-size: 1rem;
        padding: 0.75rem 1.5rem;
      }

      .proof-points {
        flex-direction: column;
        align-items: center;
      }

      .map-container {
        touch-action: pan-y pinch-zoom;
      }
    }

    /* Faint Green Fade Border */
    .edge-fade {
      position: relative;
    }

    .edge-fade::before,
    .edge-fade::after {
      content: "";
      position: absolute;
      left: 0;
      width: 100%;
      height: 20px;
      background: linear-gradient(to bottom, rgba(46, 125, 50, 0.2), transparent);
      pointer-events: none;
    }

    .edge-fade::after {
      top: auto;
      bottom: 0;
      background: linear-gradient(to top, rgba(46, 125, 50, 0.2), transparent);
    }
  </style>
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Lato:wght@400;700&family=Merriweather:wght@400;700&family=Montserrat:wght@400;700&family=Open+Sans:wght@400;700&family=Roboto:wght@400;700&display=swap" rel="stylesheet">
</head>
<body>
  <div class="edge-fade">
    <section class="hero">
      <div class="hero-content">
        <h1>Golf Site Scout: Spot Risks in Minutes, Save Thousands</h1>
        <p>Instant maps reveal contamination, floods, and wetlands before you commit</p>
        <button>
          Buy Your Map Now
          <span class="launch-price">Launch Price: $75—Buy Now</span>
        </button>

        <div class="map-container">
          <div class="map-placeholder">
            Interactive Map Coming Soon
          </div>
          <div class="risk-layers">
            <button>Floods</button>
            <button>Wetlands</button>
            <button>Contamination</button>
          </div>
        </div>
      </div>
    </section>

    <section class="middle">
      <div class="proof-points">
        <div class="proof-point">EPA-backed data</div>
        <div class="proof-point">Spot wetlands fast</div>
        <div class="proof-point">Avoid costly delays</div>
      </div>

      <div class="trust-section">
        <p>Powered by USGS/FEMA</p>
        <p>Satisfaction Guaranteed</p>
      </div>

      <div class="form-container">
        <h2>See a Sample Map</h2>
        <input type="email" placeholder="Enter your email">
        <button>Submit</button>
      </div>

      <div class="testimonial">
        "Found a flood zone in seconds—saved $15K" - John, Architect
      </div>
    </section>

    <footer class="footer">
      <button>
        Buy Your Map Now
        <span class="launch-price">Launch Price: $75—Buy Now</span>
      </button>
    </footer>
  </div>
</body>
</html>
